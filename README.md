# Automated-Research-Agent
A LangGraph-based research agent that searches for information, generates structured reports, evaluates them with quality gates, and iteratively improves them using stateful workflows, conditional routing, checkpointing, and thread-based persistence.
